import { describe, it, expect } from 'vitest';

describe('Exam related functions', () => {
	it('should calculate based on the formula', () => {
		expect(true).toBeTruthy();
	});
});

describe('email related functions', () => {
	it('should trigger email send after requestion reset password', () => {
		expect(true).toBeTruthy();
	});
});

describe('error handling related functions', () => {
	it('should catch the error and return a custom error class', () => {
		expect(true).toBeTruthy();
	});
});
